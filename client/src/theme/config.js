// Appearance picker toggle. Flip APPEARANCE_ENABLED back to true to restore the
// background + theme selector (the AppearanceBar) and per-user persistence.
// While disabled, the app is locked to the forced scheme/background below.
export const APPEARANCE_ENABLED = false
export const FORCED_SCHEME_ID = 'mist'
export const FORCED_BACKGROUND_ID = 'grain'
